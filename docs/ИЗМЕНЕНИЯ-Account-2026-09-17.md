# Изменения Perry — Account (17.09.2026, вечер)

Документ по разделу **личный кабинет**: Wishlist, My orders, Account settings — дизайн по скринам + FE + BE.

Скриншоты: [docs/screenshots/README.md](./screenshots/README.md)  
Репозитории:
- Frontend: https://github.com/ITSTEP-PERRY/perry-front (`feature/figma-storefront-port`)
- Backend: https://github.com/ITSTEP-PERRY/Back_end_for_our_poroject (`feature/categories-facets-figma-storefront`)

---

## Что сделано

### Frontend (`perry-front`)
- `AccountShell` — сайдбар, breadcrumbs
- `/account/orders`, `/account/wishlist`, `/account/settings`
- Wishlist API + Remove confirm
- Orders + Details modal (статусы Figma)
- Account settings: name / password / email+OTP / logout / delete
- PDP wish list → API

### Backend (`Perry.Api`)
- `GET/POST/DELETE /api/wishlist`
- Миграция `AddWishlistAndOrderShipping` (WishlistItems + order shipping fields)
- `PUT /auth/me`, `PUT /auth/me/password`, `POST/PUT …/email`, `DELETE /auth/me`

### Скрины
- Сохранены существующие `docs/screenshots/01`–`33` (витрина / auth / legal / admin)
- Добавлены **14** новых Account: `docs/screenshots/account/01`–`14`

---

## Проверка

```bash
dotnet run --launch-profile http   # :5272
npm run dev                        # :3000 → /account/*
```
