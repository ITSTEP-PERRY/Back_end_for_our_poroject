# Скопируйте в `.env` и при необходимости измените значения.
# Файл `.env` не коммитьте (секреты).

SA_PASSWORD=Perry_Dev_Passw0rd!
JWT_KEY=PerryDevSecretKey_ChangeMe_32chars!!

# true = письма в лог API (демо); false = реальный SMTP (#14)
SMTP_USE_STUB=true
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_FROM=
SMTP_USERNAME=
SMTP_PASSWORD=
